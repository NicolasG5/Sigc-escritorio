<UserControl x:Class="WPF_LoginForm.Views.Customer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_LoginForm.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Copiar estilos y recursos de Atencion.xaml aquí -->
        <SolidColorBrush x:Key="TextBox.Static.Border" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="TextBox.Focus.Border" Color="{StaticResource AccentColor}"/>
        <Style x:Key="TextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource TextBox.Static.Border}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="AllowDrop" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border CornerRadius="10" x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.Focus.Border}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="btnBuscarStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="10" x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DataGridStyle" TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="{StaticResource color8}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource MediumGrayBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="{StaticResource HeaderBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="{x:Type DataGridCell}">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="true">
                    <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootGrid">
        <Border CornerRadius="10" Background="{StaticResource panelColor}">
            <Grid x:Name="GridCliente" Visibility="Visible">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Foreground="{StaticResource titleColor1}" FontSize="30" HorizontalAlignment="Left" VerticalAlignment="Bottom" Grid.Column="2" Margin="65,0,0,5"><Run Text="Gestion de "/><Run Language="es-cl" Text="solicitudes"/></TextBlock>
                <TextBlock Text="Buscar Cliente:" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="1" Grid.Row="2" FontSize="15" Foreground="{StaticResource titleColor1}" Margin="0,0,10,0"/>
                <TextBox TextAlignment="Center" Background="Transparent" BorderBrush="{StaticResource AccentBrush}" Grid.Column="2" Grid.Row="2" FontSize="20" Style="{DynamicResource TextBoxStyle}" TextChanged="TextBox_TextChanged" x:Name="Buscar"/>
                <Button Background="{StaticResource AccentBrush}" BorderBrush="Transparent" Grid.Column="4" Grid.Row="2" Cursor="Hand" Width="150" x:Name="BtnCrearCliente" Style="{DynamicResource btnBuscarStyle}" Click="Agregar">
                    <TextBlock Text="Agregar Cita" Foreground="White" FontSize="15"/>
                </Button>
                <DataGrid Grid.Row="4" Grid.Column="1" Grid.RowSpan="2" Grid.ColumnSpan="4" x:Name="GridDatos" AutoGenerateColumns="False" Background="{StaticResource color8}" Foreground="{StaticResource DarkBrush}" FontSize="15" CanUserAddRows="False" CanUserResizeColumns="True" CanUserResizeRows="False" CanUserSortColumns="False" RowBackground="Transparent" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource MediumGrayBrush}" Style="{DynamicResource DataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Acciones" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Consultar" x:Name="BtnConsultar" Background="{StaticResource SuccessBrush}" Width="85" Height="28" Margin="2" BorderBrush="Transparent" Foreground="White" FontWeight="SemiBold" Style="{DynamicResource btnBuscarStyle}" Click="Consultar" CommandParameter="{Binding IdCita}" ToolTip="Consultar cita"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="ID Cita" Width="50" Binding="{Binding IdCita}"/>
                        <DataGridTextColumn Header="Fecha" Width="100" Binding="{Binding FechaCita}"/>
                        <DataGridTextColumn Header="Hora Inicio" Width="80" Binding="{Binding HoraInicio}"/>
                        <DataGridTextColumn Header="Hora Fin" Width="80" Binding="{Binding HoraFin}"/>
                        <DataGridTextColumn Header="Motivo" Width="120" Binding="{Binding MotivoConsulta}"/>
                        <DataGridTextColumn Header="Observaciones" Width="120" Binding="{Binding Observaciones}"/>
                        <DataGridTextColumn Header="Paciente" Width="120" Binding="{Binding NombrePaciente}"/>
                        <DataGridTextColumn Header="Empleado" Width="120" Binding="{Binding NombreEmpleado}"/>
                        <DataGridTextColumn Header="Servicio" Width="120" Binding="{Binding NombreServicio}"/>
                        <DataGridTextColumn Header="Estado" Width="120" Binding="{Binding NombreEstado}"/>
                        <DataGridTextColumn Header="Confirmación" Width="100" Binding="{Binding CodigoConfirmacion}"/>
                        <DataGridTextColumn Header="Creación" Width="120" Binding="{Binding FechaCreacion}"/>
                        <DataGridTextColumn Header="Modificación" Width="120" Binding="{Binding FechaModificacion}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
        <Frame x:Name="FrameCustomer" NavigationUIVisibility="Hidden" Background="Transparent" BorderThickness="0" Visibility="Collapsed" />
    </Grid>
</UserControl>
