<Window x:Class="WPF_LoginForm.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:WPF_LoginForm.ViewModels" xmlns:viewModels="clr-namespace:WPF_LoginForm.Views"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        mc:Ignorable="d"
        Title="MainWindowView" Height="700" Width="1300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:HomeViewModel}">
            <viewModels:HomeView/>

        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:CustomerViewModel}">
            <viewModels:Customer/>

        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:Customer}">
            <viewModels:Customer/>

        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:CustomerView3}">
            <viewModels:CustomerView3/>

        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:CustomerView4}">
            <viewModels:CustomerView4/>

        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:CustomerView5}">
            <viewModels:CustomerView5/>

        </DataTemplate>


        <DataTemplate DataType="{x:Type viewModel:Atencion}">
            <viewModels:Atencion/>

        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:Cita}">
            <viewModels:Cita/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:Seguimiento}">
            <viewModels:Seguimiento/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:TratamientoG}">
            <viewModels:TratamientoG/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:MedicacioG}">
            <viewModels:MedicacionG/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:ReportesG}">
            <viewModels:ReportesG/>
        </DataTemplate>

        <!-- NUEVO: DataTemplate para Gestion de Usuarios -->
        <DataTemplate DataType="{x:Type viewModel:GestionUsurarios}">
            <viewModels:GestionUsurarios/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:ControlSolicitudes}">
            <viewModels:ControlSolicitudes/>

        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:AgregarViewModel}">
            <viewModels:AgregarView/>

        </DataTemplate>




        <!--<DataTemplate DataType="{x:Type viewModel:Addpage}">
            <viewModels:Addpage/>

        </DataTemplate>-->

    </Window.Resources>

    <!-- Region de contendio -->
    <Border CornerRadius="10"
             BorderThickness="1">

        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource winBorderColor1}" Offset="0"/>
                <GradientStop Color="{StaticResource winBorderColor2}" Offset="0.5"/>
                <GradientStop Color="{StaticResource winBorderColor3}" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="627*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <!--Navegation Menu-->
                <ColumnDefinition Width="250"/>
                <!--Content Section-->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--Navigation Menu-->
            <Border CornerRadius="10,0,0,10"
                    Grid.Column="0" Grid.RowSpan="2">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                        <GradientStop Color="{StaticResource secondaryBackColor1}" Offset="0"/>
                        <GradientStop Color="{StaticResource secondaryBackColor2}" Offset="1"/>

                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel>
                    <!--Logo-->
                    <StackPanel Orientation="Horizontal"
                                Height="35"
                                Margin="15,25,0,40">

                        <TextBlock Text="SIGC"
                                   Foreground="{StaticResource titleColor1}"
                                   FontSize="20"
                                   FontFamily="Montserrat"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"/>

                    </StackPanel>
                    <!--Menu Button-->
                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color1}"
                                 Command="{Binding ShowHomeViewCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="Home" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Dashboard" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <!--<RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color2}"
                                 Command="{Binding ShowCustomerCommand}">
                                
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="UserGroup" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Customers2" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>-->

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color3}"
                                 Command="{Binding ShowCustomerCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="Envelope" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Creacion de solicitud" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <!--<RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color5}"
                                 Command="{Binding ShowCustomerView3Command}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="UserGroup" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Tabla de prueba" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>-->
                    <!--<RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color5}"
                                 Command="{Binding ShowAddpageCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="PlusCircle" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Agregar" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>-->

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color4}"
                                 Command="{Binding ShowCustomerView4Command}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="UserGroup" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Mantenedor de Empleados" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>


                    <RadioButton Style="{StaticResource menuButton}"
             Tag="{StaticResource color4}"
             Command="{Binding ShowCustomerView5Command}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="UserGroup" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Mantenedor de Paciente" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <!--<RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color8}"
                                 Command="{Binding ShowConfirmarSolicitudCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="UserGroup" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Mantenedor Clientes" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>-->

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color9}"
                                 Command="{Binding ShowControlSolicitudesCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="FlorinSign" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Gestion de Solicitudes" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color9}"
                                 Command="{Binding ShowCitaCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="FlorinSign" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Gestion de Citas" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color9}"
                                 Command="{Binding ShowAtencionCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="FlorinSign" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Gestion de Atencion" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color10}"
                                 Command="{Binding ShowSeguimientoCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="FlorinSign" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Gestion de Seguimiento" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource menuButton}"
             Tag="{StaticResource color10}"
             Command="{Binding ShowTratamientoCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="FlorinSign" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Gestion de Tratamiento" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource menuButton}"
             Tag="{StaticResource color10}"
             Command="{Binding ShowMedicacionCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="FlorinSign" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Gestion de Medicacion" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>


                    <RadioButton Style="{StaticResource menuButton}"
             Tag="{StaticResource color10}"
             Command="{Binding ShowReportesCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="FlorinSign" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Gestion de Reportes" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <!-- NUEVO: Boton para Gestion de Usuarios -->
                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color3}"
                                 Command="{Binding ShowGestionUsuariosCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="Users" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Gestion de Usuarios" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>

                    <!--<RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color2}"
                                 Command="{Binding ShowAgregarViewCommand}">

                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="Quinscape" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Revision clientes" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>-->


                </StackPanel>



            </Border>

            <!--Main Section-->
            <Border CornerRadius="0,10,10,0"
                    Grid.Column="1" Grid.RowSpan="2">
                <Border.Background>
                    <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                        <GradientStop Color="{StaticResource primaryBackColor1}" Offset="0"/>
                        <GradientStop Color="{StaticResource primaryBackColor2}" Offset="1"/>

                    </LinearGradientBrush>
                </Border.Background>
                <Grid>

                    <Grid.RowDefinitions>
                        <!--Control bar-->
                        <RowDefinition Height="25"/>
                        <!--header / caption-->
                        <RowDefinition Height="35"/>
                        <!--Content Section / Child views-->
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!--Control bar-->
                    <StackPanel x:Name="pnlControlBar"
                                Grid.Row="0"
                                Orientation="Horizontal"
                                FlowDirection="RightToLeft"
                                Background="Transparent"
                                Margin="0,0,5,0"
                                MouseLeftButtonDown="pnlControlBar_MouseLeftButtonDown"
                                MouseEnter="pnlControlBar_MouseEnter">
                        <Button x:Name="btnClose"
                                Style="{StaticResource controlButton}" 
                                Tag="{StaticResource color4}"
                                Click="btnClose_Click">
                            <fa:IconImage Icon="Xmark" Style="{StaticResource controlButtonIcon}"/>

                        </Button>

                        <Button x:Name="btnMaximize"
                                Style="{StaticResource controlButton}" 
                                Tag="{StaticResource color6}"
                                Click="btnMaximize_Click">
                            <fa:IconImage Icon="Square" Style="{StaticResource controlButtonIcon}"/>

                        </Button>

                        <Button x:Name="btnMinimize"
                                Style="{StaticResource controlButton}" 
                                Tag="{StaticResource color6}"
                                Click="btnMinimize_Click">
                            <fa:IconImage Icon="WindowMinimize" Width="12" Style="{StaticResource controlButtonIcon}"/>

                        </Button>

                    </StackPanel>
                    <!--Header / Caption-->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <!--Caption-->
                        <StackPanel Grid.Column="0"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">
                            <fa:IconImage Icon="{Binding Icon}"
                                          Height="20"
                                          Width="20"
                                          Foreground="{StaticResource titleColor2}"
                                          Margin="35,0,10,0"/>

                            <TextBlock Text="{Binding Caption}"
                                       Foreground="{StaticResource titleColor2}"
                                       FontSize="16"
                                       FontFamily="Montserrat"
                                       FontWeight="Medium"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <!--User options-->
                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    FlowDirection="RightToLeft"
                                    VerticalAlignment="Center"
                                    Margin="0,0,10,0">
                            <Button Style="{StaticResource iconButton}"
                                    Tag="{StaticResource color1}">
                                <fa:IconImage Icon="AngleDown"
                                              Height="12" Width="12"
                                              Style="{StaticResource iconButtonIcon}"/>
                            </Button>
                            <TextBlock Text="{Binding CurrentUserAccount.DisplayName}"
                                       Foreground="{StaticResource titleColor3}"
                                       FontFamily="Montserrat"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>
                            <Ellipse Height="35" Width="35"
                                     Stroke="{StaticResource color2}"
                                     StrokeThickness="2"
                                     Margin="10,0,10,0">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="/Images/Perfil.png"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <Button Style="{StaticResource iconButton}"
                                    Tag="{StaticResource color3}">
                                <fa:IconImage Icon="Clock" Style="{StaticResource iconButtonIcon}"/>
                            </Button>
                            <Button Style="{StaticResource iconButton}"
                                    Tag="{StaticResource color4}">
                                <fa:IconImage Icon="Envelope" Style="{StaticResource iconButtonIcon}"/>
                            </Button>
                            <Button Style="{StaticResource iconButton}"
                                    Tag="{StaticResource color5}">
                                <fa:IconImage Icon="Bell" Style="{StaticResource iconButtonIcon}"/>
                            </Button>
                            <Button Style="{StaticResource iconButton}"
                                    Tag="{StaticResource color6}"
                                    Command="{Binding LogoutCommand}">
                                <fa:IconImage Icon="SignOut" Style="{StaticResource iconButtonIcon}"/>
                                
                            </Button>
                        </StackPanel>

                    </Grid>

                    <!--Content section / child views-->

                    <ContentControl Content="{Binding CurrentChildView}"
                                    Grid.Row="2"
                                    Margin="25"/>
                </Grid>

            </Border>

        </Grid>




    </Border>


</Window>
