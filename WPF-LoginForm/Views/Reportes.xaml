<Page x:Class="WPF_LoginForm.Views.Reportes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_LoginForm.Views" xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Reportes"
      Background="Transparent">

    <Page.Resources>

        <!--REGION FocusVisual-->
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--endregion-->
        <!--REGION BtnStyle-->
        <SolidColorBrush x:Key="Button.Static.Background" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="{StaticResource MediumGray}"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFE0E0E0"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="BtnStyle" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="10" x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--ENDREGION-->
        <!--REGION TEXTBOX-->

        <SolidColorBrush x:Key="TextBox.Static.Border" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="TextBox.Focus.Border" Color="{StaticResource AccentColor}"/>
        <Style x:Key="TextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource TextBox.Static.Border}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="AllowDrop" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border CornerRadius="10" x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.Focus.Border}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsInactiveSelectionHighlightEnabled" Value="true"/>
                        <Condition Property="IsSelectionActive" Value="false"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="SelectionBrush" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
        <!--ENDREGION-->
        <!--REGION COMBOBOX-->
        <LinearGradientBrush x:Key="ComboBox.Static.Background" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="{StaticResource SecondaryColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource SecondaryColor}" Offset="1.0"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="ComboBox.Static.Border" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="ComboBox.Static.Editable.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ComboBox.Static.Editable.Border" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="ComboBox.Static.Editable.Button.Background" Color="Transparent"/>
        <SolidColorBrush x:Key="ComboBox.Static.Editable.Button.Border" Color="Transparent"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Glyph" Color="{StaticResource DarkColor}"/>
        <LinearGradientBrush x:Key="ComboBox.MouseOver.Background" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="{StaticResource SecondaryColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource SecondaryColor}" Offset="1.0"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Border" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Editable.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Editable.Border" Color="{StaticResource AccentColor}"/>
        <LinearGradientBrush x:Key="ComboBox.MouseOver.Editable.Button.Background" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="{StaticResource SecondaryColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource SecondaryColor}" Offset="1.0"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Editable.Button.Border" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Glyph" Color="{StaticResource DarkColor}"/>
        <LinearGradientBrush x:Key="ComboBox.Pressed.Background" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource PrimaryColor}" Offset="1.0"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="ComboBox.Pressed.Border" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Editable.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Editable.Border" Color="{StaticResource PrimaryColor}"/>
        <LinearGradientBrush x:Key="ComboBox.Pressed.Editable.Button.Background" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource PrimaryColor}" Offset="1.0"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="ComboBox.Pressed.Editable.Button.Border" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Glyph" Color="{StaticResource MediumGray}"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Background" Color="#FFF0F0F0"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Border" Color="#FFD9D9D9"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Editable.Background" Color="#FFEFEFEF"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Editable.Border" Color="#FFD9D9D9"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Editable.Button.Background" Color="Transparent"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Editable.Button.Border" Color="Transparent"/>
        <SolidColorBrush x:Key="ComboBox.Static.Glyph" Color="{StaticResource DarkColor}"/>
        <!-- Keep existing ComboBox templates/styles -->

    </Page.Resources>
    <Border CornerRadius="10"
 BorderThickness="1"
 Background="{StaticResource panelColor}">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource winBorderColor4}" Offset="0"/>
                <GradientStop Color="{StaticResource winBorderColor5}" Offset="0.5"/>
                <GradientStop Color="{StaticResource winBorderColor4}" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Grid Margin="0,0,-1,0">
            <!--REGION COLS & ROWS-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="80*"/>
                <ColumnDefinition Width="119*"/>
                <ColumnDefinition Width="101.004"/>
                <ColumnDefinition Width="99"/>
                <ColumnDefinition Width="80*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="23"/>
                <RowDefinition Height="35*"/>
                <RowDefinition Height="7.629"/>
                <RowDefinition Height="32*"/>
                <RowDefinition Height="4.789"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>
            <!--ENDREGION-->
            <!--REGION TITULO-->
            <TextBlock x:Name="Titulo"
           Text="Reporte"
           Grid.Column="2"
           Grid.Row="1"
           FontSize="30"
           VerticalAlignment="Top"
           HorizontalAlignment="Center" 
           Foreground="{StaticResource titleColor1}" Height="40" Width="185"/>
            <!--ENDREGION-->
            <!--REGION BOTONES-->
            <!--REGION REGRESAR-->
            <Button x:Name="BtnRegresar"
        Content="Regresar"
        Grid.Column="1"
        Grid.Row="1"
        Cursor="Hand"
        Background="{StaticResource SecondaryBrush}"
        Style="{DynamicResource BtnStyle}"
        Click="Regresar" Margin="10,5,50,15"/>
            <!--ENDREGION-->
            <!--REGION CREAR-->
            <Button x:Name="BtnCrear"
        Content="Crear"
        Grid.Row="19"
        Grid.Column="5"
        Cursor="Hand"
        Background="{StaticResource PrimaryBrush}"
        Style="{DynamicResource BtnStyle}"
        Visibility="Hidden"
        Click="Crear" Margin="30,8,30,0" Grid.RowSpan="2"/>
            <!--ENDREGION-->
            <!--REGION ELIMINAR-->
            <Button x:Name="BtnEliminar"
        Content="Eliminar"
        Grid.Row="19"
        Grid.Column="5"
        Cursor="Hand"
        Background="{StaticResource ErrorBrush}"
        Style="{DynamicResource BtnStyle}"
        Visibility="Hidden"
        Click="Eliminar" Margin="30,8,30,0" Grid.RowSpan="2"/>
            <!--ENDREGION-->
            <!--REGION MODIFICAR-->
            <Button x:Name="BtnActualizar"
        Content="Actualizar"
        Grid.Row="19"
        Grid.Column="5"
        Cursor="Hand"
        Background="{StaticResource PrimaryBrush}"
        Style="{DynamicResource BtnStyle}"
        Visibility="Hidden"
        Click="Actualizar" Margin="30,8,30,0" Grid.RowSpan="2"/>
            <!--ENDREGION-->
            <!--ENDREGION-->
            <!--REGION CONTENIDO-->
            <TextBlock Grid.Row="2"
           Grid.Column="1"
           Text="Nombre Paciente:"
           HorizontalAlignment="Left"
           VerticalAlignment="Top"
           Foreground="{StaticResource titleColor1}" Height="16" Width="102"/>
            <TextBox Grid.Row="1"
         Grid.Column="1"
         x:Name="tbNombre" Style="{DynamicResource TextBox}"
         HorizontalContentAlignment="Center" Margin="139,42,53,1" Grid.ColumnSpan="3" RenderTransformOrigin="0.5,0.5" Grid.RowSpan="3">
                <TextBox.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-0.133"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </TextBox.RenderTransform>
            </TextBox>

            <TextBox Grid.Row="4"
         Grid.Column="1"
         x:Name="tbFecha"
         Style="{DynamicResource TextBox}"
         HorizontalContentAlignment="Center" Margin="138,10,51,25" Grid.RowSpan="5" Grid.ColumnSpan="3"/>
            <TextBlock Grid.Row="6"
           Grid.Column="1"
           Text="Evolucion del caso:"
           HorizontalAlignment="Left"
           VerticalAlignment="Top"
           Foreground="{StaticResource titleColor1}" Margin="0,4,0,0" Height="16" Width="105"/>
            <TextBlock Grid.Row="10"
    Text="Descripcion Seguimientos :"
    HorizontalAlignment="Left"
    VerticalAlignment="Top"
    Foreground="{StaticResource titleColor1}" RenderTransformOrigin="0.59,3.962" Height="16" Width="137" Margin="17,2,0,0" Grid.ColumnSpan="2"/>
            <TextBox Grid.Row="8"
    Grid.Column="1"
    x:Name="tbFecha_Copiar1"
    Style="{DynamicResource TextBox}"
    HorizontalContentAlignment="Center" Margin="138,15,0,0" Grid.RowSpan="8" HorizontalAlignment="Left" Width="311" Grid.ColumnSpan="3" Height="144" VerticalAlignment="Top"/>
            <TextBlock Grid.Row="16"
    Text="Tratamientos:"
    HorizontalAlignment="Left"
    VerticalAlignment="Top"
    Foreground="{StaticResource titleColor1}" RenderTransformOrigin="0.59,3.962" Height="16" Width="137" Margin="0,5,0,0" Grid.Column="1" Grid.RowSpan="3"/>
            <TextBox Grid.Row="19"
    Grid.Column="1"
    x:Name="tbFecha_Copiar4"
    Style="{DynamicResource TextBox}"
    HorizontalContentAlignment="Center" Margin="96,8,94,10" Grid.RowSpan="4" Grid.ColumnSpan="3"/>
            <TextBlock Grid.Row="19"
    Text="Conclusion Final:"
    HorizontalAlignment="Left"
    VerticalAlignment="Top"
    Foreground="{StaticResource titleColor1}" RenderTransformOrigin="0.59,3.962" Margin="2,4,0,0" Height="28" Width="87" Grid.RowSpan="2" Grid.Column="1"/>
            <TextBox Grid.Row="15"
        Grid.Column="1"
        x:Name="tbFecha_Copiar"
        Style="{DynamicResource TextBox}"
        HorizontalContentAlignment="Center" Margin="88,32,101,1" Grid.RowSpan="4" Grid.ColumnSpan="3"/>
            <TextBlock Grid.Row="16"
        Text="Medicaciones:"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        Foreground="{StaticResource titleColor1}" RenderTransformOrigin="0.59,3.962" Height="16" Width="137" Margin="5,5,0,0" Grid.Column="3" Grid.ColumnSpan="2" Grid.RowSpan="3"/>
            <TextBox Grid.Row="15"
        Grid.Column="3"
        x:Name="tbFecha_Copiar5"
        Style="{DynamicResource TextBox}"
        HorizontalContentAlignment="Center" Margin="81,27,0,20" Grid.RowSpan="6" Grid.ColumnSpan="3"/>
            <!--ENDREGION-->
            <!--REGION ENVIAR-->
            <Button x:Name="BtnPdf"
            Content="Pdf"
            Grid.Row="20"
            Grid.Column="5"
            Width="100"
            Cursor="Hand"
            Background="{StaticResource PrimaryBrush}"
            Style="{DynamicResource BtnStyle}"
            Click="Pdf" Margin="30,19,30,9" Grid.RowSpan="3"/>
            <Button x:Name="BtnExcel"
            Content="Excel"
            Grid.Row="20"
            Grid.Column="4"
            Width="100"
            Cursor="Hand"
            Background="{StaticResource AccentBrush}"
            Style="{DynamicResource BtnStyle}"
            Click="Excel" Margin="19,18,140,10" Grid.RowSpan="3" Grid.ColumnSpan="2"/>
            <!--ENDREGION-->



        </Grid>

    </Border>
</Page>
